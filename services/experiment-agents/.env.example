# Server only. Generate your own token; never reuse another work's credentials.
EXPERIMENT_AGENT_TOKEN=
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=
# Default model attempt budget; at most two attempts within a stage.
LLM_TIMEOUT_SECONDS=30
